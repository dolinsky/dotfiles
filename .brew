#!/usr/bin/env bash

brew update
brew upgrade

# Install GNU core utilities (those that come with OS X are outdated)
brew install coreutils
echo "Don’t forget to add $(brew --prefix coreutils)/libexec/gnubin to \$PATH."
# Install GNU `find`, `locate`, `updatedb`, and `xargs`, g-prefixed
brew install findutils
# Install Bash 4
brew install bash
brew install bash-completion2
sudo bash -c "echo /usr/local/bin/bash >> /private/etc/shells"

# Install zsh
brew install zsh
sudo bash -c "echo /usr/local/bin/zsh >> /private/etc/shells"
# make brew bash the default shell
sudo chsh -s /usr/local/bin/bash

# Install wget with IRI support
brew install wget --enable-iri

# Install more recent versions of some OS X tools
brew tap homebrew/dupes
brew install homebrew/dupes/grep

# Install python
brew install python --with-tcl-tk

# Install some useful binaries
brew install ack
brew install autoconf
brew install automake
brew install awscli
#brew install ctags
brew install --HEAD universal-ctags/universal-ctags/universal-ctags
brew install dos2unix
#brew install brew-pip
#brew install elasticsearch
#==> Caveats
#Data:    /usr/local/var/elasticsearch/elasticsearch_dolinsky/
#Logs:    /usr/local/var/log/elasticsearch/elasticsearch_dolinsky.log
#Plugins: /usr/local/var/lib/elasticsearch/plugins/
#Config:  /usr/local/etc/elasticsearch/
#
#To have launchd start elasticsearch at login:
#    ln -sfv /usr/local/opt/elasticsearch/*.plist ~/Library/LaunchAgents
#Then to load elasticsearch now:
#    launchctl load ~/Library/LaunchAgents/homebrew.mxcl.elasticsearch.plist
#Or, if you don't want/need launchctl, you can just run:
#    elasticsearch --config=/usr/local/opt/elasticsearch/config/elasticsearch.yml
brew install fping
brew install fontforge
brew install freetype
brew install gawk --with-default-names
brew install gdbm
brew install gettext
brew install gfortran
brew install gist
brew install git
brew install graphviz
# gnu-sed -- PATH="/usr/local/opt/gnu-sed/libexec/gnubin:$PATH"
# gnu-sed -- MANPATH="/usr/local/opt/gnu-sed/libexec/gnuman:$MANPATH"
brew install gnu-sed --with-default-names
brew install htop-osx
brew install hub
brew install kdiff3
brew install libevent
brew install libidn
brew install libpng
brew install lynx
brew install ngrep
brew install readline
brew install reattach-to-user-namespace
brew install sqlite
brew install siege
# to get around the socket limit on OSX
# sysctl net.inet.tcp.msl
#net.inet.tcp.msl: 15000
# sudo sysctl -w net.inet.tcp.msl=1000
#net.inet.tcp.msl: 15000 -> 1000
#Run siege.config to create the ~/.siegerc config file.

brew install ssh-copy-id
brew install the_silver_searcher
brew install tig
brew install tmux --with-utf8proc
brew install tree
brew install vim --with-gettext --with-luajit --with-python3
brew install wget
brew install zmq --universal

# ruby 'n friends
brew install rbenv ruby-build
# after installing rbenv, ensure the following 2 lines are in .bash_profile
# export PATH="$HOME/.rbenv/bin:$PATH"
# eval "$(rbenv init -)"

# for orange3
brew install freetds --with-krb5 --with-msdblib --with-unixodbc
# install: brew cask install *name*
# cleanup:  brew update && brew upgrade brew-cask && brew cleanup && brew cask cleanup
# Install native apps

function installcask() {
	if brew cask info "${@}" | grep "Not installed" > /dev/null; then
		brew cask install "${@}"
	else
		echo "${@} is already installed."
	fi
}

installcask iterm2
installcask docker #installs docker for mac
installcask vlc
installcask colloquy
installcask dropbox
installcask evernote
#installcask mattr-slate #https://github.com/mattr-/slate
#installcask quicksilver
installcask jumpcut
#installcask hipchat
#installcask pgadmin3
installcask gpgtools
installcask 1password
#installcask vagrant
#installcask virtualbox
#https://github.com/dotless-de/vagrant-vbguest
